import { describe, it, expect } from '@jest/globals';

// Exemplo de teste simples para garantir que o ambiente está funcionando

describe('Sanity check', () => {
  it('should add numbers correctly', () => {
    expect(2 + 2).toBe(4);
  });
});
